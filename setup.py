"""
Banana production network
"""
from setuptools import setup, find_packages

setup(
    name="banana_production_network",
    version="0.1.0",
    packages=find_packages()
    #packages=find_packages(where="src"),
    #package_dir={"": "src"},
)
